name: antigravity-traffic
app: python@3.12
options:
  ports: 5000
  healthCheck: /api/health
  env:
    - name: PORT
      value: "5000"
    - name: AUTO_START_BACKGROUND
      value: "1"
    - name: AUTO_START_SCHEDULER
      value: "1"
    - name: ENABLE_CLOUD_TRAFFIC
      value: "0"
    - name: ENABLE_CLOUD_RANK_SWEEP
      value: "1"
    - name: CLOUD_RANK_SWEEP_INTERVAL_HOURS
      value: "3"
    - name: RANK_USE_API
      value: "1"
    # Cloudtype 대시보드에서 시크릿으로 설정 (저장소에 키 넣지 말 것):
    # SERPAPI_KEY, GOOGLE_API_KEY, GOOGLE_CSE_ID, GCS_BUCKET, CRON_SECRET
    - name: DAILY_RANK_HOUR
      value: "9"
    - name: DAILY_RANK_MINUTE
      value: "30"
    - name: DATA_DIR
      value: "/data"
    - name: GUNICORN_TIMEOUT
      value: "3600"
  install: pip install -r requirements.txt gunicorn
  start: gunicorn -c gunicorn_conf.py app:app
context:
  git:
    url: https://github.com/canon40/traffic.git
    branch: main
